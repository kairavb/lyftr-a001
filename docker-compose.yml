services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - DATABASE_URL=sqlite:////data/app.db
      - LOG_LEVEL=INFO
    volumes:
      - .:/app
      - data:/data
volumes:
  data:
